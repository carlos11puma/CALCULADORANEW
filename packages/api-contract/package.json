{
  "name": "@calculadora-comisiones/api-contract",
  "version": "1.0.0",
  "private": true,
  "description": "Contrato OpenAPI de Calculadora de Comisiones y los tipos TypeScript generados a partir de él — consumido como dependencia de workspace por backend-api y mobile-app, sin publicar a un registro npm externo.",
  "type": "module",
  "main": "dist/types.js",
  "types": "dist/types.d.ts",
  "scripts": {
    "generate-types": "tsx scripts/generate-types.ts",
    "build": "npm run generate-types && tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^2.1.9",
    "openapi-typescript": "^7.4.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
